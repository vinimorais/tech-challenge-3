import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
  chartContainer: {
    marginTop: 30,
    width: '100%',
    alignItems: 'center',
  },
  chartTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 10,
  },
});

export default styles;
